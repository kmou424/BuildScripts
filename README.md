# Usage

```bash
[File]main.sh
-b, -build, b, build                  Run build process.
-c, -clean, c, clean                  Run clean process.
-l, -log, log, logcater, -logcater    Logcater.
-j, j, thread, -thread                Change build thread.
```
